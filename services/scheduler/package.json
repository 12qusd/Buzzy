{
  "name": "@buzzy/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Buzzy Today cron job orchestrator",
  "main": "./dist/scheduler.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/scheduler.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint passed'",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@buzzy/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "vitest": "^3.0.0"
  }
}
